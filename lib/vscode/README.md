# Code-Tomoshibi（VS Code 分叉）

本目录是 Code-Tomoshibi 对 microsoft/vscode 1.132.0 的分叉，只保留网页版需要的部分，桌面版相关代码已裁掉。
怎么构建、怎么跑，见仓库根目录的 README。
上游的 `LICENSE.txt`（MIT）原样保留，本分叉同样按 MIT 授权。

This directory is Code-Tomoshibi's fork of microsoft/vscode 1.132.0, trimmed down to what the web build needs; the desktop-only code has been removed.
For build and run instructions, see the README at the repository root. The upstream `LICENSE.txt` (MIT) is kept unchanged and this fork is MIT-licensed too.
